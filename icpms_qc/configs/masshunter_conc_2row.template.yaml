# Kept so existing commands and saved scripts keep working.
#
# These were two templates until both were taught to read Conc. and CPS columns,
# after which they were identical in every field. Two copies of one layout drift
# apart; one canonical file with names pointing at it does not.
id: masshunter_conc_2row
alias_of: masshunter_2row
